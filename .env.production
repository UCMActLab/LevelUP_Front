# Production environment variables - For Docker deployment
# Note: This will be built into the app, so must match the Docker service name and port
VITE_API_BASE_URL=http://159.69.190.201:8079
VITE_APP_ENV=production
VITE_APP_NAME=LevelUP
VITE_API_TIMEOUT=10000
